<?php 
	$sum = 0;                               // 定义变量$sum，用于记住累加的和
	for ($i = 1; $i <= 4; $i++) {       // $i 的值会在1~4之间变化
		$sum += $i;               // 实现$sum与$i 的累加
	}
	echo '$sum = '.$sum;      // 打印累加的和
?>
